1. ¿Cuál es el comando para leer un archivo csv?
#A. read.cvs()
#B. read.csv()***
#C. csv.read()

2.Empareje las siguientes funciones con su definición:
# dim() <- Muestra la dimension del vector
# colnames() <- Muestra los nombres de las columnas
# str() <- Muestra la estructura del vector
# length() <- Muestra la longitud del vector
# dimnames() <- Muestra los nombres de las columnas y las filas

3. ¿Con qué función se obtienen los valores únicos de un vector?
#A. unique()***
#B. unico()
#C. only()
  
4. ¿De qué forma podemos seleccionar las variables de un data frame?
#A. $***
#B. [,]
#C. [;]
#D. /

5.Seleccione la forma correcta de crear una nueva varaible "x" con el valor 1 en un data frame existente "dat"
#A. dat$x <- 1***
#B. dat <- $x
#C. dat <- colname(x=1)

6. ¿Con que función podemos reemplazar los valores en un vector?
#A. replace()***
#B. reemplace()
#C. change()
#D. generate()
  
7. ¿Cuál es el símbolo que se usa en la función boxplot() para graficar una variable por cada grupo?
#A. + 
#B. ~***
#C. :
#D. =
  
8. ¿Con cuál función obtenemos las posiciones de los datos que cumplen una función especificada en el código?
#A. who()
#B. which()***
#C. wich()
#D. what()
  
9. ¿Con qué funciones podemos saber si un valor es numérico y caracter?
#A. character() & is.numeric()
#B. character() & numeric()
#C. is.character() & is.numeric()***
#D. is:numeric() & is:character()
  
10. ¿Con qué función se puede crear rangos de una variable continua en R?
#A. ranges()
#B. cut()***
#C. rangos()
#D. str()
  
11. ¿Cuál es la estructura del seguiente data frame?
df <- data.frame(data=1:12, col=2)
#A. Un data frame con los datos del uno al doce ordenados por fila
#B. Un data frame con dos columnas, una con los números del uno al doce y otra con el número 2 repetido 12 veces***
#C. Un data frame con los datos del uno al doce ordenados por columna
#D. Una matriz con los números del uno al doce
  
12. ¿Cuál es la diferencia entre un data frame y una matriz? 
#A. Una matriz almacena dos o más tipos de datos, los data frames, no
#B. Un data frame almacena datos numéricos y caracteres, las matrices sólo datos numúricos
#C. Ambos pueden contener cualquier tipo de dato, las matrices sólo un tipo de dato a la vez, y los data frames datos mixtos (lógicos, caracteres, numéricos)*** 